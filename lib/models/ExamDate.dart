class ExamDate {
  int id;
  String examSubject;
  String dateTime;
  bool done;

  double latitude;
  double longitude;

  // String address;

  ExamDate(this.id, this.examSubject, this.dateTime, this.done, this.longitude,
      this.latitude);
}
